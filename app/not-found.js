export default function NotFound() {
    return (
        <main className="not-found">
            <h1>Not found</h1>
            <p>Unfortunately, We could not process through the requested page or resource.</p>
        </main>
    )
}